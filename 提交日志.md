# 提交日志

## 格式

```
## [日期]

**类型**：初始化 / 功能 / 修复 / 重构 / 测试 / 文档

**摘要**：简述本次提交内容

**主要改动**：
- 改动1
- 改动2

**验证命令**：
\`\`\`
命令
\`\`\`

**验证结果**：
\`\`\`
输出
\`\`\`
```

---

## 2026-07-11

**类型**：初始化

**摘要**：建立项目基础规则和文档

**主要改动**：
- 创建 README.md，包含项目目标、范围、技术栈和开发阶段
- 创建 AGENTS.md，包含编码、分层、测试和提交规范
- 创建 提交日志.md，建立日志记录格式
- 创建 .gitignore，忽略构建产物和敏感文件

**验证命令**：
```
cat README.md AGENTS.md 提交日志.md .gitignore
```

**验证结果**：
四个文件存在，内容与项目范围一致，无业务代码。

---

## 2026-07-11

**类型**：初始化

**摘要**：创建最小可编译C++20命令行项目

**主要改动**：
- 创建 CMakeLists.txt，使用 C++20 标准，启用 Wall/Wextra/Wpedantic/Werror
- 创建 include/index_fund_monitor/version.hpp，定义项目名称和版本号
- 创建 src/main.cpp，实现 --help、--version、无参数显示帮助、未知参数报错
- 创建 tests/、config/、data/ 目录占位
- 不依赖 libcurl、SQLite、JSON 和任何第三方库

**验证命令**：
```
cmake -S . -B build -G Ninja -DCMAKE_CXX_COMPILER=g++
cmake --build build
./build/index_fund_monitor
./build/index_fund_monitor --help
./build/index_fund_monitor --version
./build/index_fund_monitor --unknown
```

**验证结果**：
- 编译成功，零警告
- 无参数 → 显示帮助，退出码0
- --help → 显示帮助，退出码0
- --version → 显示版本号 0.1.0，退出码0
- --unknown → 标准错误输出错误信息，退出码1

---

## 2026-07-11

**类型**：功能

**摘要**：实现独立命令行参数解析模块

**主要改动**：
- 创建 include/index_fund_monitor/cli/parser.hpp，定义 CommandType、ParseResult、Parser
- 创建 src/cli/parser.cpp，实现命令匹配、选项解析、必填校验和已知选项校验
- 重构 src/main.cpp，使用 Parser 解析并分发命令，未实现命令输出"尚未实现"
- 更新 CMakeLists.txt，加入 parser 源文件和 test_parser 测试目标
- 创建 tests/test_parser.cpp，覆盖 52 个测试用例
- 支持 12 个命令：init、fund add/list/remove、update、show、screen、alert add/list/remove/check、export
- 支持 8 个通用选项：--database、--market、--symbol、--name、--benchmark-code、--benchmark-name、--output、--all

**验证命令**：
```
cmake -S . -B build -G Ninja -DCMAKE_CXX_COMPILER=g++
cmake --build build
./build/test_parser.exe
./build/index_fund_monitor.exe
./build/index_fund_monitor.exe --help
./build/index_fund_monitor.exe --version
./build/index_fund_monitor.exe unknown
./build/index_fund_monitor.exe init --invalid
./build/index_fund_monitor.exe fund add --market CN --name TestETF
./build/index_fund_monitor.exe show --symbol 510300 --symbol 510050
```

**验证结果**：
- 0 编译警告
- 52 测试全部通过，0 失败
- 正确命令可识别，未实现命令输出"尚未实现"
- 缺少必填选项、未知命令、未知选项、重复选项均返回错误和退出码1

---

## 2026-07-11

**类型**：功能

**摘要**：建立A股指数ETF监控器领域模型

**主要改动**：
- 创建 include/index_fund_monitor/models/models.hpp，定义 Market、Exchange 枚举和 Fund、FundListing、BenchmarkIndex、FundBenchmark、IndexValuation、FundDistribution、DailyPrice、DataSource、CollectionResult 结构体
- 创建 src/models/models.cpp，实现枚举与字符串互转函数
- 创建 include/index_fund_monitor/models/validation.hpp，声明 ETF代码、市场、交易所、百分比、日期格式校验函数
- 创建 src/models/validation.cpp，实现校验函数
- 创建 tests/test_models.cpp，覆盖 63 个测试用例
- 更新 CMakeLists.txt，加入模型源文件和 test_models 测试目标，修复 add_test_target 函数使用 ARGN 的问题

**验证命令**：
```
cmake -S . -B build -G Ninja -DCMAKE_CXX_COMPILER=g++
cmake --build build
./build/test_models.exe
./build/test_parser.exe
```

**验证结果**：
- 0 编译警告
- 63 模型测试全部通过，0 失败
- 52 解析器回归测试全部通过，0 失败
- 模型包含缺失值 std::optional、百分比使用小数、日期 YYYY-MM-DD 格式
- 模型中不含 SQL、HTTP 或网页解析代码

---

## 2026-07-11

**类型**：功能

**摘要**：实现SQLite数据库初始化和schema创建

**主要改动**：
- 创建 include/index_fund_monitor/db/database.hpp，定义 RAII Database 类（支持move语义）
- 创建 src/db/database.cpp，使用 SQLite C API 实现连接管理、错误处理和事务
- 创建 include/index_fund_monitor/db/schema.hpp，声明 schema 初始化函数
- 创建 src/db/schema.cpp，实现11张表的CREATE TABLE并记录 schema_version
- 重构 src/main.cpp，init 命令接入真实逻辑：自动创建目录、打开数据库、初始化 schema
- 创建 tests/test_database.cpp，覆盖 34 个测试用例
- 更新 CMakeLists.txt，链接 sqlite3 库，加入 test_database 目标

**验证命令**：
```
cmake -S . -B build -G Ninja -DCMAKE_CXX_COMPILER=g++
cmake --build build
./build/test_database.exe
./build/index_fund_monitor init
./build/index_fund_monitor init
./build/index_fund_monitor init --database custom_test.db
```

**验证结果**：
- 0 编译警告
- 34 数据库测试全部通过，0 失败
- 149 回归测试全部通过（63模型 + 52解析 + 34数据库）
- init 命令可正常创建，重复执行幂等
- 支持 --database 自定义路径
- 外键约束已启用

---

## 2026-07-11

**类型**：功能

**摘要**：实现A股ETF关注列表管理（添加、列出、删除）

**主要改动**：
- 创建 include/index_fund_monitor/db/fund_repository.hpp，定义 FundRecord、FundRepository 和 deduce_exchange
- 创建 src/db/fund_repository.cpp，实现 add、remove、list、find、exists 方法和交易所代码推断
- 重构 src/main.cpp，添加 open_db 辅助函数，接入 fund add / fund list / fund remove 命令实现
- 创建 tests/test_fund_repository.cpp，覆盖 34 个测试用例
- 更新 CMakeLists.txt，加入 fund_repository 源文件和 test_fund_repository 测试目标

**验证命令**：
```
cmake -S . -B build -G Ninja -DCMAKE_CXX_COMPILER=g++
cmake --build build
./build/test_fund_repository.exe
./build/test_database.exe
./build/test_models.exe
./build/test_parser.exe
./build/index_fund_monitor init
./build/index_fund_monitor fund add --market CN --symbol 510300 --name "沪深300ETF"
./build/index_fund_monitor fund list
./build/index_fund_monitor fund remove --market CN --symbol 510300
./build/index_fund_monitor fund add --symbol 123 --name bad
```

**验证结果**：
- 0 编译警告
- 34 基金仓库测试全部通过，0 失败
- 183 所有测试全部通过（34基金 + 34数据库 + 63模型 + 52解析）
- fund add：添加成功，重复添加报错，非法代码校验通过
- fund list：显示已添加ETF，空列表正确提示
- fund remove：删除成功，不存在ETF报错
- 交易所根据代码前缀自动推断（51/56/58→SSE，15/16→SZSE）

---

## 2026-07-11

**类型**：功能

**摘要**：建立数据源适配器接口和Mock实现

**主要改动**：
- 创建 include/index_fund_monitor/data/data_result.hpp，定义 ProviderError
- 创建 4 个数据源接口：IFundDirectoryProvider、IIndexValuationProvider、IFundDistributionProvider、IPriceProvider
- 创建 include/index_fund_monitor/data/json_util.hpp，实现 JSON 文件加载和字段读取工具
- 创建 4 个 Mock Provider 实现，均从本地 fixture JSON 文件读取数据
- 创建 7 个 fixture JSON 样本数据文件
- 创建 tests/test_data_providers.cpp，覆盖 45 个测试用例
- 更新 CMakeLists.txt，加入 test_data_providers 测试目标

**验证命令**：
```
cmake -S . -B build -G Ninja -DCMAKE_CXX_COMPILER=g++
cmake --build build
./build/test_data_providers.exe
```

**验证结果**：
- 0 编译警告
- 45 数据源测试全部通过，0 失败
- Mock 覆盖场景：正常成功、字段缺失、空列表、网络错误、格式错误、文件不存在、多态调用

---

## 2026-07-11

**类型**：功能

**摘要**：使用libcurl实现轻量HTTP客户端

**主要改动**：
- 创建 include/index_fund_monitor/net/http_client.hpp，定义 HttpRequestOptions、HttpResponse、IHttpClient 接口、HttpClient 实现和 MockHttpClient
- 创建 src/net/http_client.cpp，使用 libcurl 实现 HTTP GET，支持超时、重试、大小限制、HTTPS证书校验
- 创建 src/net/mock_http_client.cpp，实现可配置的 Mock 客户端
- 创建 tests/test_http_client.cpp，覆盖 23 个测试用例
- 更新 CMakeLists.txt，链接 libcurl 库，加入 test_http_client 目标

**验证命令**：
```
cmake -S . -B build -G Ninja -DCMAKE_CXX_COMPILER=g++
cmake --build build
./build/test_http_client.exe
```

**验证结果**：
- 0 编译警告
- 23 HTTP 客户端测试全部通过，0 失败
- Mock 覆盖场景：成功、重试（先失败后成功）、超时、连接错误、大小限制、4xx错误
- 选项可配置：超时、大小、重试次数
- HttpClient 通过接口可被 Mock 替换